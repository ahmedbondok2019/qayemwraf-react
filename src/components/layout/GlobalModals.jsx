import React from "react";

export const GlobalModals = () => {
	// Placeholder container for global dialogs/modals
	return <div id="global-modals" aria-live="assertive" className="relative z-modal" />;
};

export default GlobalModals;
