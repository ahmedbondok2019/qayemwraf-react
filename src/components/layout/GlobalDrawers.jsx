import React from "react";

export const GlobalDrawers = () => {
	// Placeholder drawer container for global drawers (e.g. mini-cart, mobile navigation drawer)
	return <div id="global-drawers" aria-live="polite" className="relative z-modal" />;
};

export default GlobalDrawers;
